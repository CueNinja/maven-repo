stages:
  - build
  - deploy

deploy:
  stage: deploy
  image: node
  script:
    - apt-get update && apt-get install -y curl unzip
    - npm install
    - ./download_artifacts.sh
    - npm run deploy
